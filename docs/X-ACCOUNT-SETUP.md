# X Account Setup — Inkd Protocol

## Bio Varianten (160 Zeichen max)

**Option 1 (empfohlen — bereits live):**
> If it's not inked, it's not yours.

*58 Zeichen. Sagt alles ohne ein Wort zu erklären.*

---

**Option 2 (technischer):**
> on-chain project registry for developers and ai agents. register once. own forever. built on base.

*98 Zeichen. Für technische audience die sofort verstehen wollen.*

---

**Option 3 (ecosystem-first):**
> the ownership layer for base. register projects on-chain. push versions to arweave. transfer trustlessly.

*105 Zeichen. Positioniert inkd als base-native infra.*

---

**Option 4 (agent-focused):**
> agents can transact. now they can own. permanent project registry on base. $inkd.

*83 Zeichen. Targets AI agent developer audience directly.*

---

**Option 5 (minimal):**
> provenance for code. ownership on-chain. built on base.

*55 Zeichen. Maximum compression.*

---

## Pinned Tweet (Launch-Ready)

```
the ownership layer for ai agents is live on base mainnet.

register a project: lock 1 $inkd. permanent.
push a version: 0.001 eth. stored on arweave. forever.
transfer ownership: 0.005 eth. one transaction. done.

292 sdk tests. 159 contract tests. no admin keys. no pause function.

if it's not inked, it's not yours.

inkdprotocol.com
```

*Post als standalone tweet direkt nach dem launch thread. Bleibt gepinnt.*

---

## Profile Details

- **Username:** @inkdprotocol
- **Display Name:** inkd protocol
- **Website:** https://inkdprotocol.com
- **Location:** Base
- **Bio (live):** If it's not inked, it's not yours.

---

## Header Image Konzept

**Beschreibung für Designer:**

Schwarzer Hintergrund (#0A0D1E — gleiche Farbe wie Website).
Links: Großer, leicht verblassender Tintentropfen (Inkd Visual Identity).
Rechts: In weißer Monospace-Schrift, 3 Zeilen:

```
inkd protocol
on-chain ownership for code
built on base
```

Unten rechts: Base Logo in 20% Opacity.
Keine weiteren Elemente. Kein Farbgradient. Kein Glow-Effekt.
Größe: 1500x500px.

*Alternativ: Screenshot von `inkd publish` CLI output auf schwarzem Terminal.*

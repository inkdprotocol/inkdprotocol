# X Account Setup — Inkd Protocol

---

## Handle

`@inkdprotocol`

---

## Bio Variants (160 chars max)

Pick the one that matches the current phase. Update at milestones.

**Variant A — Pre-Launch (technical audience)**
```
on-chain project registry on base. 1 $INKD to register. 0.001 eth per version. permanent. 292 sdk tests. 159 contract tests. building in public.
```
*139 chars. For: builders, degens who respect test coverage.*

**Variant B — Agent-First framing**
```
permanent on-chain registry for code and ai agents. base. 1 $INKD to register. every version is forever. every transfer is provable.
```
*133 chars. For: AI/agent builder audience.*

**Variant C — Minimal / Confident**
```
on-chain project registry. base. 1 $INKD = 1 registered project. permanent. immutable. 292 sdk tests.
```
*101 chars. For: when confidence speaks louder than explanation.*

**Variant D — Post-Launch with metrics**
```
on-chain project registry on base. [X] projects registered. [Y] versions pushed. 1 $INKD to register. fair launch. building.
```
*Replace [X] and [Y] with live numbers at launch.*

**Variant E — Token-Centric (fair launch phase)**
```
$INKD — on-chain project registry on base. 1B supply. fair launch. register code permanently. burn on use. 100% test coverage.
```
*127 chars. For: token launch window.*

---

## Profile Image

**Primary:** Inkd Protocol logo mark — dark background, high contrast.

**Alt (launch week):** Animated or static image with "mainnet live" overlay — swap back to standard after 72h.

---

## Header Image — Concept & Text

**Concept:**
Dark background (near-black, #0d0d0d or deep navy). Monospace font throughout. Two columns:

Left side — CLI terminal output:
```
$ inkd init my-project
✓ project created
$ inkd publish
✓ version 0.1.0 pushed to arweave
  tx: 0xf3a2...b891
  cost: 0.001 eth
$ inkd info my-project
  owner: 0x1a2b...3c4d
  versions: 1
  registered: block 18923401
```

Right side — three stats in large type:
```
292
sdk tests

159
contract tests

100%
coverage
```

Bottom right corner, small: `inkd protocol — base`

**Mood:** Looks like actual output from a real terminal. No gradients, no stock imagery, no abstract art. It's a protocol for devs. The header should feel like reading a README.

---

## Pinned Tweet (Launch-Ready, Full Text)

```
we built an on-chain project registry on base.

here's how it works:

— 1 $INKD locks to register a project. permanent. non-reversible.
— 0.001 eth per version push → stored on arweave forever
— 0.005 eth per ownership transfer → goes to treasury
— every project, every version, every transfer: on-chain. immutable.

this isn't decentralized storage for photos.
this is a registry for code. for ai agents. for anything that needs provenance.

if you built it, inkd proves you built it.
if you transferred it, inkd proves when and to whom.
if you pushed v47, inkd proves what v47 was.

292 sdk tests. 159 contract tests. 100% coverage. mainnet live on base.

cli: inkd init / inkd publish / inkd info / inkd transfer / inkd list

inkdprotocol.com
```

*Why this works as a pinned tweet:*
- Explains the mechanism without buzzwords
- Real numbers (292, 159, 0.001 eth) do the credibility work
- Covers devs AND agent builders without switching tone
- CTA is the URL, not "follow us"
- Leads with what it does, not what it "is"

---

## Account Settings

- **Location:** Base (onchain)
- **Website:** inkdprotocol.com
- **Sensitive content:** Off
- **DMs:** Open for builders, closed for spam — consider filtering

---

## First 5 Actions After Account Goes Live

1. Post pinned tweet, pin it immediately
2. Follow: @jessepollak, @LitProtocol, @graphprotocol, @base, @0xPolygon, @coinbase, @VitalikButerin, top 20 AI agent builders on CT
3. Reply to 3 relevant threads (agent memory, onchain infra) before posting anything else
4. Post first "build log" tweet (not the pinned — a casual one about what shipped this week)
5. Do not post twice in the first hour — let the pinned tweet breathe

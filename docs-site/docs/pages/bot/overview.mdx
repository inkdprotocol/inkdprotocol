---
title: Telegram Bot
description: Upload to Arweave, register on Base, pay in USDC — all inside Telegram.
---

# Telegram Bot

[@inkdbot](https://t.me/inkdbot) is the easiest way to use inkd. No SDK, no CLI, no terminal. Just Telegram.

You send a file or text. The bot uploads it to Arweave, registers it on Base, and charges your wallet in USDC — all in one flow.

## Commands

| Command | Description |
|---|---|
| `/start` | Connect or create a wallet |
| `/tutorial` | Interactive guided tour (4 steps) |
| `/wallet` | Show wallet address, ETH & USDC balance |
| `/export_key` | Export your private key (bot-managed wallets) |
| `/upload_text` | Upload text content |
| `/upload_repo` | Archive a GitHub repo |
| `/my_projects` | View your projects |
| `/search <name>` | Search public projects |
| `/history` | View your last 5 uploads |
| `/links` | Website, Twitter, buy $INKD |
| `/cancel` | Cancel current action |
| `/help` | Show all commands |

## How it works

```
User → Telegram Bot
           │
           ├── /v1/upload  ──────────→ Arweave (permanent storage)
           ├── /v1/projects ─────────→ InkdRegistry on Base (x402 USDC)
           └── /v1/projects/*/versions → same
```

You pay in USDC on Base. The bot holds a bot-managed wallet for you (encrypted, AES-256-GCM). No custody risk — you own the key.

## Pricing

| Action | Cost |
|---|---|
| Create project | $0.10 USDC |
| Push version | Arweave cost × 1.20 (min $0.10) |

Prices are on-chain. The bot shows the exact cost before every upload.

## Infrastructure

Running on `john-server` (DigitalOcean FRA1) under pm2 as `inkd-bot`.

```bash
ssh root@167.172.102.246
pm2 status inkd-bot
pm2 logs inkd-bot --lines 50
pm2 restart inkd-bot
```

/**
 * @file index.ts
 * @description Inkd Memory System — agents storing their brain as tokens.
 *              Own the wallet, own the brain. Transfer, fork, or burn memories.
 */

export { AgentMemory } from "./AgentMemory";
export type {
  Memory,
  MemoryCategory,
  MemoryQuery,
  BrainExport,
  Checkpoint,
  SaveOptions,
} from "./AgentMemory";

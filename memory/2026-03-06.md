# 2026-03-06 — Daily Log

## Session Summary

### x402 Full Flow — Komplett gebaut und deployed

**Kontext:** Session startete mit hängengebliebenen Vercel Deploys (SIGTERM) und fehlenden Upload/SDK/CLI Komponenten.

---

## Was gebaut wurde

### 1. API — `/v1/upload` Endpoint
- **File:** `api/src/routes/upload.ts`
- POST /v1/upload — Content zu Arweave via Irys (Server Wallet = `0x210bDf52...`)
- GET /v1/upload/price?bytes=N — Kostenabschätzung (1 KB ≈ $0.0018 USDC)
- Akzeptiert: `application/json` mit base64-encoded data + contentType
- Max 50 MB, Tags: Content-Type, App-Name, File-Name
- In `index.ts` registriert: `/v1/upload` (kein x402 — free endpoint)

### 2. SDK — `ProjectsClient`
- **File:** `sdk/src/ProjectsClient.ts`
- Exported aus `sdk/src/index.ts`
- x402 payment via `@x402/fetch` + `@x402/evm` (require() wrappers wegen fehlender Types)
- Methoden:
  - `createProject(params)` → $5 USDC via x402
  - `pushVersion(projectId, params)` → $2 USDC via x402
  - `upload(data, opts)` → POST /v1/upload (free)
  - `getProject(id)` → free read
  - `listProjects(opts)` → free read
  - `estimateUploadCost(bytes)` → free
- viem version conflict zwischen CLI/SDK → gelöst mit `as any` cast

### 3. CLI — x402 Flow
- **project.ts** — `cmdProjectCreate` rewritten: kein direkter Contract-Call mehr, geht via API mit x402
- **version.ts** — `cmdVersionPush` rewritten: `--file <path>` uploadet zu Arweave + pushed on-chain, `--hash ar://...` für pre-uploaded
- `cmdProjectTransfer` + `cmdProjectCollab` entfernt (via Safe stattdessen)
- Fehlende `error` import in `index.ts` gefixt

### 4. E2E Test
- **File:** `api/examples/e2e-mainnet.ts`
- Full flow: upload → createProject → pushVersion → readProject
- `SKIP_UPLOAD=1` flag für wenn Irys nicht funded

### 5. SDK Build Fix
- `tsconfig.json` → `exclude: ["src/__tests__"]` hinzugefügt
- `types: ["node"]` für Buffer support
- `@x402/fetch` + `@x402/evm` als dependencies installiert

---

## Deploy

- **Commit:** `4388a8c` auf `main` ✅
- **Vercel:** deployed von workspace root (`/`) wegen `vercel.json` mit `api/src/index.ts` path
- **Live:** `https://api.inkdprotocol.com/v1/upload/price?bytes=1024` → `{"bytes":1024,"costUsdc":"1844","costUsd":"$0.0018"}`
- **Health:** `{"ok":true,"service":"@inkd/api","version":"0.1.0"}` ✅

---

## Offene TODOs (Stand EOD)

- ❌ `setInkdToken($TEST CA)` via Buyback Safe aufrufen
- ❌ API .env updaten: REGISTRY_ADDRESS + TREASURY_ADDRESS
- ❌ $INKD via Clanker (erst wenn $TEST sauber läuft)
- ❌ Security Audit vor Public Launch
- ❌ E2E Test mit echtem Agent-Wallet auf Mainnet laufen lassen
- ❌ Docs updaten (Mintlify) mit upload endpoint + ProjectsClient

---

## Technische Notizen

- **Irys Upload:** Server Wallet hat nur ~0.0015 ETH. Reicht für wenige Test-Uploads. Für Produktion: Wallet aufladen oder Irys credits kaufen.
- **x402/evm require() Workaround:** `@x402/fetch` und `@x402/evm` haben keine sauberen TypeScript Declarations → via `require()` importiert mit manuellen Typannotationen.
- **Vercel Deploy:** Muss von `/Users/hazar/.openclaw/workspace` (root) aus deployen, nicht von `/api/`. Das `vercel.json` im root zeigt auf `api/src/index.ts`.
